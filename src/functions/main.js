const { Activity, Presence } = require('discord.js');
const { Client } = require('../client.js');

/**
 *
 * @param {Presence} info
 * @param {Activity} activity
 * @param {Client} client
 */
module.exports = async function(info, activity, client) {

}